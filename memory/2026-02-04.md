- Feishu 附件接收 MVP（默认方式）：用户在飞书聊天里直接发送“附件文件”给机器人 → OpenClaw/Feishu 接入层以 inbound media 形式接收并自动落盘到本机（如 `/Users/guyuanyuan/.openclaw/media/inbound/<name>---<uuid>.<ext>`）→ 后续处理直接基于本地文件读取/解析/转换/总结；不依赖飞书云空间。
  - 备注：`file_key` JSON 属于后续增强场景（当未落盘或仅拿到 key 时可再做 OpenAPI 下载），但 MVP 不依赖 file_key。
