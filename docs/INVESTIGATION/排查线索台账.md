# 益语官网 · 排查线索台账

> 记线索不记结论。线索 = tier-0 客观事实(花真功夫挖出、耐久);推测挂在线索上、随时可被推翻。
> 每次排查先读此表(固定=免费地基不重验 / 墓碑=禁区别重走 / 待验·暂信=本轮靶子),收敛后回写。
> 自信度阶梯:待验(0)→暂信(1-2 独立验证)→固定(≥3 独立验证未翻)→墓碑(被推翻)。只认独立验证(换 agent/方法/数据角度)。

---

## 课题:文章页"先旧封面→后新封面"封面跳动 (2026-06-19 起)

### 固定线索 (≥3 独立验证 / 强一手)

- **C1 · 封面有两套来源,详情页显示时异步二选一**(暂信→固定方向)
  `ArticleDetailPage.tsx:169-208` hero 的 background-image 与 `<img>` 同用表达式 `aiCoverUrl || displayArticle.coverImage`;`aiCoverUrl` 来自挂载后 `useEffect`(`:63-72`)异步 `fetch('/api/admin-ai/manifest',{cache:'no-store'})` → `setAiEntry`。首屏用 `coverImage`,manifest 回来切 `aiCoverUrl` = 视觉跳。
  验证:① 代码精读(主) ② R2 子agent逐行核对 background/img 同源无 transition ③ 线上 manifest GET 200/19条全有cover。

- **C2 · 线上快照里文章 coverImage = 预设封面,≠ AI 封面**(一手·2 次)
  `/api/content-snapshot` 19 篇 insights 的 `coverImage` 全是 `/uploads/cover-presets/insight/...微信图片.png`;AI 封面单独在 `/uploads/ai-generated/articles/<id>/cover.jpg`。两者从不合流。
  验证:① 线上 curl content-snapshot 解析(主) ② manifest∩snapshot 19/19 命中。

- **C3 · AI 封面文件真实落盘且 URL 200,生产基址 `/uploads/ai-generated` 已生效**(一手)
  `/var/www/yiyu-site-data/uploads/ai-generated/articles/insight_.../cover.jpg` 存在;`https://www.yiyu.love/uploads/ai-generated/.../cover.jpg` → 200;`aiAssets.ts` 生产基址命中。`aiArticleDir(id)+'/cover.jpg'` 与详情页 aiCoverUrl 字节一致。
  验证:① 线上 ls 磁盘 ② curl URL 200 ③ 代码 aiAssets 基址核对。

- **C4 · cover.jpg 被浏览器强缓存 30 天 → 二次访问几乎不闪,首次未缓存才明显**(一手)
  `cache-control: public, max-age=2592000` + etag/last-modified。
  验证:R2 子agent `curl -sIL` 真实响应头。

- **C5 · 改 coverImage 字段会被 admin 保存回写污染 canonical(护栏缺口真实存在)**(一手·含线上后端)
  后台编辑器 = `AdminDashboard.tsx` 的 `InsightFormModal`;打开文章 `setCoverImage(article.coverImage)` 取的是缓存值;**无 coverPresetId 选择器**(全仓编辑器零引用);不碰封面直接保存 → `articleData.coverImage` 原样 → `saveInsightDirect` → `syncKey('/api/content-sync')` → 线上 `pg-content-api.mjs` `ON CONFLICT DO UPDATE SET cover_image=EXCLUDED.cover_image` **整列逐字覆盖**,`ensureReportCover` 只守 reports 不守 insights。例外:仅当本次保存点了"恢复默认/上传替换"才不污染。
  验证:① R1 子agent 全链源码追踪 ② R1 线上只读 pg-content-api.mjs upsert 逻辑 ③ 反证搜索确认挡板不存在。

- **C6 · 启动是"先渲染后异步取数",bootstrap 不阻塞首屏(防白屏,有意设计)**(一手)
  `main.tsx:38-45` `start()=renderApp(); void refreshContentSnapshot()`(不 await)。App 无加载门控,详情页无 key 重挂、不订阅 yiyu_data_change(仅订阅登录态)。推论:热导航进文章时启动 bootstrap 已完成,故 `getInsights()` 即就绪;冷加载/硬刷新存在 bootstrap 未完成的竞速窗口。
  验证:① main.tsx/App.tsx/ArticleDetailPage 代码精读。

- **C7 · 列表页 ArticleCenterPage 同根因但时序机制不同**(暂信)
  `:35-52` 用 `aiManifestRef`(ref 非 state)+`setInterval(1000ms)` poll 触发重画,换封面延迟特征与详情页(state 驱动)不同。
  验证:① 代码精读 ② R2 复核。待补:真机/真浏览器观察列表是否可见跳动(仅 1 次代码侧)。

### 墓碑 (被推翻 / 禁区)

- **T1 · "把 AI 封面写进 coverImage(改缓存 applyAiCovers 或后端快照)就能根治且安全"——假**
  死因:违反 C5,共享快照经 admin 保存回流 → 永久污染全部文章 canonical 预设封面。当前工作树里我写的 `applyAiCovers`(dataService.ts)即此潜伏污染 bug,需撤。
  级联:任何"在 coverImage 字段上动手"的方案一律否。

### 推测(挂在线索上,随线索生死)

- **P1(暂信,挂 C1/C2/C6)**:闪烁主因 = 详情页自身异步 manifest fetch 在首屏后替换封面;热导航为主场景。
- **P2(暂信,挂 C5/T1)**:修复必须"不碰 coverImage 字段",改走显示层同步解析。
- **P3(待验,挂 C6/C7)**:冷加载/硬刷新仍可能闪(竞速窗口),任何不 await bootstrap 的方案都治不全该边缘;需真浏览器复现确认边缘严重度。

### 修复方向(打在根因 C1+C5 上,不打症状)

manifest 在 bootstrap 时存进模块级**同步缓存**,详情页/列表页**显示时同步解析** AI 封面、`coverImage` 字段保持 canonical(预设)不变。热导航场景首屏即 AI 封面无跳;冷加载保留异步兜底无回归。**禁止改 coverImage 字段(T1)**。下一步交 执行+1 收敛实施序。
