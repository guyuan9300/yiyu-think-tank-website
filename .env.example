# Copy to .env.local (do NOT commit .env.local)
# Frontend (safe to expose)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NOTE: Do NOT put service_role keys / OpenAI keys here.
# Put sensitive secrets in GitHub Actions Secrets or Supabase Edge Function secrets.
